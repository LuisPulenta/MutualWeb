@page "/tiposclientes/edit/{Id:int}"

<h3>Editar Tipo de Cliente</h3>

@if (tipoCliente is null)
{
    <Loading/>
}
else
{
    <TipoClienteForm @ref="tipoClienteForm" TipoCliente="tipoCliente" OnValidSubmit="EditAsync" ReturnAction="Return" />
}
